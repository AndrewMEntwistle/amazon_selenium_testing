written in python 3.7

Setup the below environment variable:
WEBDRIVER = path/to/chromedriver.exe

Install the dependencies in requirements.txt using pip install

Run test by using the "behave" command in the root dir